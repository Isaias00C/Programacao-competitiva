#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, M;
    cin >> N >> M;
    int K, L;
    cin >> K >> L;
    int m[N][M];

    for(int i = 0; i < N; i++){
        for(int j = 0; j < M; j++){
            cin >> m[i][j];
        }
    }

    vector<int> dir_x = {};
    vector<int> dir_y = {};
}