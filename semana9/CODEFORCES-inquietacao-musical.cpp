#include <bits/stdc++.h>
using namespace std;

int main(){
    int numLines;
    cin >> numLines;
    vector<string> trecho;

    for(int line = 0; line < numLines; line++){
        string phrase;
        getline(cin, phrase);
        trecho.push_back(phrase);
    }
    
    
}