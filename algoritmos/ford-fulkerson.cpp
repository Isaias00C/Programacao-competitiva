#include <btis/stdc++.h>
using namespace std;

vector<int> bfs(vector<vector<int>> G, int s, int t);

int main(){

}

vector<int> bfs(vector<vector<pair<int,int>>> G, int s, int t){
    vector<int> vis;
    queue<int> q;
    q.push(s);

    while (!q.empty()){
        int currNode = q.front();
        q.pop();
        
        
    }
    
}   