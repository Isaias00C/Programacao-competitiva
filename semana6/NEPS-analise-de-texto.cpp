#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<string> frases;

    while(1){
        string palavra;
        cin >> palavra;
        frases.push_back(palavra);
    }
    string teste = frases[frases.max_size() - 1];
}